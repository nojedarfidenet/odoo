<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="clinica.list">
      <field name="name">Clinica list</field>
      <field name="model">clinica.clinica</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="clinica.action_window">
      <field name="name">Clinica window</field>
      <field name="res_model">clinica.clinica</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="clinica.action_server">
      <field name="name">Clinica server</field>
      <field name="model_id" ref="model_clinica_clinica"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Clinica" id="clinica.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="clinica.menu_1" parent="clinica.menu_root"/>
    <menuitem name="Menu 2" id="clinica.menu_2" parent="clinica.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="clinica.menu_1_list" parent="clinica.menu_1"
              action="clinica.action_window"/>
    <menuitem name="Server to list" id="clinica" parent="clinica.menu_2"
              action="clinica.action_server"/>
    -->
  </data>
</odoo>