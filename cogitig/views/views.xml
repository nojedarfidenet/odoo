<odoo>
  <data>

    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cogitig.list">
      <field name="name">cogitig list</field>
      <field name="model">cogitig.cogitig</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cogitig.action_window">
      <field name="name">cogitig window</field>
      <field name="res_model">cogitig.cogitig</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cogitig.action_server">
      <field name="name">cogitig server</field>
      <field name="model_id" ref="model_cogitig_cogitig"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cogitig" id="cogitig.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cogitig.menu_1" parent="cogitig.menu_root"/>
    <menuitem name="Menu 2" id="cogitig.menu_2" parent="cogitig.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cogitig.menu_1_list" parent="cogitig.menu_1"
              action="cogitig.action_window"/>
    <menuitem name="Server to list" id="cogitig" parent="cogitig.menu_2"
              action="cogitig.action_server"/>
    -->
  </data>
</odoo>